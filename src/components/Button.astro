---
const { classes, ...rest } = Astro.props;
---

<button class:list={["btn", classes]} {...rest}>
  <slot />
</button>

<style>
  .btn {
    font: inherit;
    display: flex;
    gap: 0.23rem;
    align-items: center;
    cursor: pointer;
    border: none;
    padding: 4px 16px;
    background: none;
  }
</style>
